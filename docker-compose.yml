version: "3.9"

services:
  db:
    image: postgres:15-alpine
    container_name: unimeal-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres123}
      POSTGRES_DB: ${DB_NAME:-unimeal}
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./unimeal/backend/seed/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql
      - ./unimeal/backend/seed/seed_data.sql:/docker-entrypoint-initdb.d/02_seed_data.sql
    ports:
      - "5432:5432"

  backend:
    image: ${DOCKER_USERNAME}/unimeal-backend:latest
    container_name: unimeal-backend
    restart: unless-stopped
    depends_on:
      - db
    environment:
      PORT: 5000
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres123}
      DB_NAME: ${DB_NAME:-unimeal}
      JWT_SECRET: ${JWT_SECRET:-unimeal-secret}
    ports:
      - "5000:5000"

  frontend:
    image: ${DOCKER_USERNAME}/unimeal-frontend:latest
    container_name: unimeal-frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  pg_data: